// import { io } from "socket.io-client";
// const socket = io("http://localhost:3001"); 

// socket.on("connect", () => {
//    const myUserId = localStorage.getItem("user-ID");
//    if (myUserId) {
//       socket.emit("register", myUserId); 
//       console.log("Registered with userId:", myUserId);
//    }
// });
